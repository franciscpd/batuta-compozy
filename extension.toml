[extension]
name = "batuta"
version = "0.1.0-beta.1"
description = "Batuta, the conductor: routes CompozyOS dev-cycle work to the cheapest capable executor and never writes code itself."
# This is the first supported beta for the Loop features used by Batuta.
min_compozy_version = "0.3.0-beta.13"

[resources]
agents = ["agents"]
skills = ["resources/skills"]
loops = ["loops"]
